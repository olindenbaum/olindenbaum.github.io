int main() {
    int n, i, range;
    printf("Enter an integer: ");
    scanf("%d", &amp;n);
    printf("Enter the range: ");
    scanf("%d", &amp;range);
    for (i = 1; i &lt;= range; ++i) {
        printf("%d * %d = %d \n", n, i, n * i);
    }
    return 0;
}
