#include &lt;stdio.h&gt;
int main() {
    int n, i, sum = 0;

    do {
        printf("Enter a positive integer: ");
        scanf("%d", &amp;n);
    } while (n &lt;= 0);

    for (i = 1; i &lt;= n; ++i) {
        sum += i;
    }

    printf("Sum = %d", sum);
    return 0;
}
